name: CI
on:
  pull_request_target:
    types:
     - closed

jobs:
  build:
    runs-on: ubuntu-latest
    if: ${{ !github.event.pull_request.head.repo.fork }}
    env:
      FLAG: ${{ secrets.FLAG }}
    steps:
      - uses: actions/checkout@v4
      - run: echo $FLAG | base64 | base64
